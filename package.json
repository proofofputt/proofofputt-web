{
  "name": "proofofputt",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd apps/web && npm install && npm run build && cp -r dist/* ../.. && cp dist/index.html ../../index.html",
    "dev": "cd apps/web && npm run dev"
  },
  "dependencies": {},
  "devDependencies": {}
}